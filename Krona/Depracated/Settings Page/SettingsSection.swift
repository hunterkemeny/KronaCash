//
//  SettingsSection.swift
//  Krona
//
//  Created by Hunter Kemeny on 7/28/19.
//  Copyright © 2019 Krona Technologies Incorporated. All rights reserved.
//
/*
enum SettingsSection: Int, CaseIterable, CustomStringConvertible {
    case History
    case SocialMedia
    case Account
    case Administrative
    case Logout
    
    var description: String {
        switch self {
        case .History: return "History"
        case .SocialMedia: return "Social Media"
        case .Account: return "Account"
        case .Administrative: return "Administrative"
        case .Logout: return "Logout"
        }
    }
}

enum HistoryOptions: Int, CaseIterable, CustomStringConvertible {
    case transactionHistory
    
    var description: String {
        switch self {
        case .transactionHistory: return "Transaction History"
        }
    }
}

enum SocialMediaOptions: Int, CaseIterable, CustomStringConvertible {
    case Facebook
    case Instagram
    case Twitter
    
    var description: String {
        switch self {
        case .Facebook: return "Facebook"
        case .Instagram: return "Instagram"
        case .Twitter: return "Twitter"
        }
    }
}

enum AccountOptions: Int, CaseIterable, CustomStringConvertible {
    case Membership
    case Settings
    case Support
    
    var description: String {
        switch self {
        case .Membership: return "Membership"
        case .Settings: return "Settings"
        case .Support: return "Support"
        }
    }
}

enum AdministrativeOptions: Int, CaseIterable, CustomStringConvertible {
    case TOS
    case Privacy
    
    var description: String {
        switch self {
        case .TOS: return "Terms of Service"
        case .Privacy: return "Privacy"
        }
    }
}

enum LogoutOptions: Int, CaseIterable, CustomStringConvertible {
    case Logout
    
    var description: String {
        switch self {
        case .Logout: return "Logout"
        }
    }
}
*/
